// SAVE PATHITEM COLORS AS SWATCHES// // In an RGB Color document, this script gets the fillColor for every pathItem on the active layer and saves them as a Process color to the Swatches panel and names them base on // the RGB colors.// // Distributed under the MIT license. See http://www.opensource.org/licenses/mit-license.php for details.// Copyright (c) 2011, Dr. Woohoo!// http://DrWoohoo.com// All rights reserved.// v.01 // GENERALvar aDocument = app.activeDocument;var docLayer = aDocument.activeLayer;var aPathItems = docLayer.pathItems;var swatches = aDocument.swatches;var newSG = aDocument.swatchGroups.add();for (var i = 0; i < aPathItems.length; i++){    // Get the Color of the current aPathItem    var newColor = aPathItems[i].fillColor;              // ADD RGB COLORS    if (newColor.red != undefined)    {      // Create a new Swatch and Save the color to it        var newRGB = swatches.add();        newRGB.name = "R=" + Math.round(newColor.red) + " G="  + Math.round(newColor.green) + " B=" + Math.round(newColor.blue);        newRGB.colorType = ColorModel.PROCESS;        newRGB.color = newColor;                    // Move the new swatch to the new Group        newSG.addSwatch(newRGB);    }}